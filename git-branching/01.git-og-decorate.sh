# which commit origin/master points to
# which commit local branches points to
# which branch your head points to. Branch your head points to is your current branch.
# next commit is created as children of your branch, and your branch moves forwad.
git log --decorate
git log --all
git log --graph
